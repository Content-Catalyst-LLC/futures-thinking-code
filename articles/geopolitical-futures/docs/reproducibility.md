# Reproducibility Notes

Run:

```bash
bash run_smoke_tests.sh
```

Default Python uses only the standard library. The advanced Python workflow uses pandas, numpy, and matplotlib and exits gracefully if they are not installed.
